import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  Container: {
    flex: 1,
    justifyContent: "center",
    alignContent: "center",
    backgroundColor: "gray",
  },

  title: {
    color: "#093463",
    textAlign: "center",
    fontSize: 20,
    fontWeight: "bold",
    marginBottom: 20,
    textDecorationColor: "#190808",
  },
  text: {
    fontSize: 18,
    marginBottom: 20,
  },
});

export default styles;
